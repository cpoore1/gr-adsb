id: adsb_framer
label: framer
category: '[adsb]'

templates:
  imports: import adsb
  make: adsb.framer()

inputs:
- label: in
  domain: stream
  dtype: byte
  vlen: 1
  optional: 0

outputs:
- label: out
  domain: message
  dtype: message
  vlen: 1
  optional: 0

file_format: 1
